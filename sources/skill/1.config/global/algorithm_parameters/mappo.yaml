defaults:
  - defaults/default_mappo@_here_
  - _self_

seed:
  seed: 1

device:
  cuda: False
  cuda_deterministic: False

train:
  eval_interval: 10
  num_env_steps: 20000000
  n_rollout_threads: 10