program: sources.skill.code.train
method: bayes
project: harl_mapdn_ppo_sweep
name: 250723_mappo_happo
metric:
  goal: maximize
  name: eval_average_episode_rewards.max
parameters:
  algorithm_parameters.model.lr:
    values: [0.0001, 0.001]
  algorithm_parameters.algo.ppo_epoch:
    values: [5, 10, 15]
  algorithm_parameters.algo.actor_num_mini_batch:
    values: [1, 2]
  algorithm:
    values: [mappo, happo]
command:
  - ${env}
  - uv
  - "run"
  - "python"
  - "-m"
  - ${program}
  - "wandb.wandb_group=250709_sweep2_1"
  - "++wandb.wandb_project=mw_harl_sweep2_1"
  - "model.save_group=latest"
  - "scenario=raw"
  - "environment=mapdn"
  - "++algorithm_parameters.train.n_rollout_threads=1"
  - "algorithm_parameters.train.num_env_steps=96000"
  - ${args_no_hyphens}